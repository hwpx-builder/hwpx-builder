# 기여 안내

한글 문서를 다루는 도구는 만드는 사람보다 **깨진 걸 발견하는 사람**이 훨씬
많습니다. 제보 하나가 코드 한 줄보다 값질 때가 많으니 편하게 열어 주세요.

이 문서는 규칙이 많아 보이지만, 실제로 지켜야 하는 건 아래 네 가지입니다.
나머지는 전부 설명입니다.

1. **라이선스 경계를 넘지 않는다** — 제한 패키지는 `render.py`, `convert.py` 안에서만
2. **확인 못 한 것은 OK로 표시하지 않는다** — `NOT VERIFIED`는 실패가 아니라 정직함
3. **함정을 고쳤으면 `references/gotchas.md`에 남긴다**
4. **실제 제출 문서·개인정보를 커밋하지 않는다**

---

## 목차

- [버그 제보](#버그-제보)
- [개발 환경](#개발-환경)
- [지켜야 할 규칙 4가지](#지켜야-할-규칙-4가지)
- [코드 스타일](#코드-스타일)
- [테스트 — 이 저장소에서 "동작한다"의 뜻](#테스트--이-저장소에서-동작한다의-뜻)
- [Pull Request](#pull-request)
- [라이선스 동의](#라이선스-동의)

---

## 버그 제보

### 문서가 이상하게 나올 때

한글에서 열었더니 글자가 겹치거나, 표가 밀리거나, 칸이 넘칠 때입니다. 이런
문제는 **재현 파일이 없으면 사실상 고칠 수 없습니다.** 다음을 같이 올려 주세요.

- 문제가 생긴 `.hwpx` 파일 (또는 최소 재현 코드)
- 그 파일을 만든 스크립트 또는 코드 조각
- `verify()` 출력 전체
- 한글 버전 (예: 한글 2022) 과 운영체제
- 스크린샷 — **눈으로 보이는 문제는 스크린샷이 가장 빠릅니다**

```python
from hwpxkit import verify
print(verify("문제.hwpx").render())         # 새로 만든 문서
print(verify("수정본.hwpx", baseline="원본.hwpx").render())   # 편집한 문서
```

> **올리기 전에 파일을 한 번 열어 확인해 주세요.** 실제 사업계획서나 제출 서류를
> 그대로 올리면 이름·연락처·기관명이 공개 저장소에 영구히 남습니다. 값을 바꾼
> 최소 재현 파일을 만들어 주시는 게 가장 좋고, 어려우면 이슈에 상황만 적어
> 주세요. 파일은 따로 받을 방법을 안내하겠습니다.

### 양식이 안 채워질 때

배포 양식마다 구조가 달라서 `find_label`이 칸을 못 찾는 경우가 있습니다. 이때는
어떤 라벨을 어떤 `direction`으로 찾으려 했는지, 그리고 아래 출력을 같이 올려
주세요.

```python
from hwpxkit import iter_cells
for ref in iter_cells(doc):
    print(ref.path, repr(ref.text[:40]))
```

### 기능 제안

"이런 문서를 만들고 싶은데 안 된다"는 형태가 가장 좋습니다. 만들려는 문서의
모습(사진, 스캔, 배포 양식 링크)이 있으면 훨씬 빠릅니다. 다만
[이 도구의 범위](README.md#이-도구의-범위)를 먼저 봐 주세요 — 어휘를 일부러 작게
유지하고 있어서, 함수를 늘리는 제안은 근거가 필요합니다.

---

## 개발 환경

```powershell
git clone https://github.com/hwpx-builder/hwpx-builder.git
cd hwpx-builder
python -m venv .venv
.\.venv\Scripts\Activate.ps1

pip install -e ".[preview,hwp,fast,images]"   # 개발용은 전부 넣는다
```

macOS / Linux는 `source .venv/bin/activate`입니다.

`-e`(editable)로 설치해야 코드를 고칠 때마다 다시 설치하지 않아도 됩니다.
개발할 때는 부가 설치를 다 넣어도 되지만, **배포되는 기본 설치에는 절대 넣지
않습니다** (아래 규칙 1).

### 한글이 없어도 기여할 수 있나요

됩니다. 구조 검사(`verify(..., render=False)`)는 한글 없이 전부 돌아가고,
`[preview]`를 설치하면 PNG 미리보기까지 됩니다. 다만 **최종 확인은 한글에서 눈으로
보는 것**이라, 한글이 없다면 PR에 그 사실을 적어 주세요. 대신 확인해 드립니다.

---

## 지켜야 할 규칙 4가지

### 규칙 1 — 라이선스 경계를 넘지 않는다

이 프로젝트가 **기본 설치는 Apache-2.0, 부가 설치는 비상업**으로 갈라져 있는 건
편의가 아니라 이 도구를 서비스로 띄우거나 회사에서 쓸 수 있게 하려는 설계입니다.
경계가 무너지면 그게 불가능해집니다.

**제한 패키지(`pyhwpxlib`, `pymupdf`)를 import 하는 파일은 딱 두 개입니다.**

| 파일 | 하는 일 |
|---|---|
| `hwpxkit/render.py` | PNG 미리보기, 렌더 검사 |
| `hwpxkit/convert.py` | 구식 `.hwp` → `.hwpx` 변환 |

다른 파일에서 이 패키지를 import 하면 안 됩니다. 두 파일 안에서도 **모듈 최상단이
아니라 함수 안에서** import 해야 합니다. 최상단에 두면 `import hwpxkit`만 해도
로드되어 버립니다.

```python
# ✅ 맞다 — convert.py 안, 함수 내부
def hwp_to_hwpx(path):
    try:
        from pyhwpxlib.hwp2hwpx import convert
    except ImportError:
        raise ConverterUnavailable(...)

# ❌ 틀렸다 — 파일 최상단
from pyhwpxlib.hwp2hwpx import convert
```

경계가 살아 있는지 확인하는 명령입니다. PR 전에 한 번 돌려 주세요.

```bash
python -c "import hwpxkit, sys; print([m for m in sys.modules if 'pyhwpx' in m or 'fitz' in m])"
# []  가 나와야 한다. 부가 설치를 한 개발 환경에서도 [] 여야 한다.
```

`pyproject.toml`의 `dependencies`에 무언가를 추가하는 건 **패키징이 아니라
라이선스 결정**입니다. 새 의존성은 라이선스를 PR 본문에 적어 주세요. 판단이
애매하면 이슈로 먼저 물어봐 주시는 게 좋습니다.

> **한 번 더 강조합니다.** `pyhwpxlib` 안의 변환 파일 3개가 Apache-2.0이라고 해서
> 변환 기능이 상업적으로 쓸 수 있는 게 아닙니다. 실행하면 같은 패키지 모듈 59개가
> 로드되고 그중 56개가 PolyForm 비상업입니다. 자세한 내용은 [`NOTICE`](NOTICE).

### 규칙 2 — 확인 못 한 것은 OK로 표시하지 않는다

`verify()`는 초록불을 많이 띄우는 게 목적이 아닙니다. **무엇을 확인했고 무엇을
확인하지 못했는지 구분하는 것**이 목적입니다.

- 실제로 검사한 항목만 `OK`
- 검사할 수단이 없으면 `NOT VERIFIED` — **이유를 함께** 적습니다
- 검사 항목을 추가할 때는, 그 검사가 **어떤 경우에 FAIL을 내는지** PR에 씁니다.
  절대 실패하지 않는 검사는 검사가 아니라 장식입니다.

```
~   highlight renders: NOT VERIFIED (렌더러가 형광펜을 아예 무시한다)
```

이유 없는 `NOT VERIFIED`는 사용자가 무시하게 되고, 한 번 무시하기 시작하면 나머지
검사 결과도 같이 신뢰를 잃습니다.

같은 이유로 **편집으로 생긴 문제와 원본이 원래 갖고 있던 문제를 구분**해야
합니다. 배포 양식은 손대기 전부터 넘친 칸을 갖고 있는 경우가 있습니다. 그걸 편집
탓으로 보고하면 오탐이 됩니다.

### 규칙 3 — 함정을 고쳤으면 기록을 남긴다

[`references/gotchas.md`](references/gotchas.md)가 이 프로젝트의 실질적인
자산입니다. 항목 하나하나가 디버깅 한 사이클을 치르고 얻은 것입니다.

HWPX 형식 때문에 생긴 버그를 고쳤다면 항목을 추가해 주세요. 형식이 아니라 단순
로직 실수였다면 추가하지 않아도 됩니다.

표기는 세 가지입니다.

| 표기 | 뜻 |
|---|---|
| ✅ | 이 저장소에서 실제로 재현하고 확인함 |
| 📋 | 소스를 읽고 판단함 (실행으로 확인하지는 못함) |
| ⚠️ | 여기서는 확인할 수단이 없음 |

**추측을 ✅로 적지 마세요.** 이 파일이 믿을 만한 이유는 표기가 정확해서입니다.
`gotchas.md`는 영어로 되어 있으니 항목도 영어로 써 주세요 (아래 [코드
스타일](#코드-스타일) 참고 — 코드 주석 규칙과 다릅니다).

### 규칙 4 — 실제 문서와 개인정보를 커밋하지 않는다

이 도구는 사업계획서·연구 보고서처럼 실명과 소속이 잔뜩 들어간 문서를 다룹니다.
공개 저장소에 한 번 들어간 파일은 커밋을 되돌려도 git 기록에 남습니다.

- 예제와 테스트 파일의 이름·기관·연락처·금액은 전부 **가상의 값**으로 씁니다
- 배포 양식(`examples/forms/`)은 **빈 양식만** 둡니다. 채워진 것은 안 됩니다
- 이미지는 출처와 라이선스를 `examples/images/README.md`에 적습니다
- `.hwpx`는 ZIP입니다. 열어 보면 안에 뭐가 들었는지 확인할 수 있습니다

---

## 코드 스타일

읽는 사람이 한국에서 한글 문서를 다루는 사람이라, 언어를 파일별로 나눴습니다.

| 대상 | 언어 |
|---|---|
| `hwpxkit/` 주석과 docstring | **한국어** |
| 함수·변수 이름 | 영어 |
| `references/gotchas.md` | 영어 |
| README, SKILL.md, 이 문서 | 한국어 |
| 커밋 메시지, PR | 한국어 또는 영어 (편한 쪽) |

그 외에는 주변 코드를 따라가 주세요. 포매터는 강제하지 않습니다.

**주석은 "무엇"이 아니라 "왜"를 씁니다.** 이 코드베이스의 주석 대부분은 그렇게
하지 않으면 안 되는 이유를 설명합니다. 그게 없으면 다음 사람이 "이거 왜 이렇게
복잡하지" 하고 되돌려 놓고, 같은 버그를 다시 만납니다.

```python
# ✅ 왜
# lxml 은 접근할 때마다 프록시를 새로 만들고 CPython 은 주소를 재사용한다.
# 그래서 id() 만 담아두면 서로 다른 요소가 같은 것으로 취급된다. 요소 자체를
# 들고 있어야 한다.

# ❌ 무엇
# 방문한 요소를 집합에 넣는다
```

또 하나. **참고한 특정 문서의 흔적을 주석에 남기지 마세요.** 이 코드는 실제
배포 양식 몇 건을 뜯어보며 만들었지만, 주석은 형식 자체를 설명해야 합니다.
"○○ 사업계획서에서는 이렇더라"가 아니라 "HWPX에서는 이렇다"로 씁니다.

---

## 테스트 — 이 저장소에서 "동작한다"의 뜻

자동화된 테스트 스위트는 아직 없습니다. 대신 **예제가 테스트 역할**을 합니다.
예제는 이 도구가 실제로 만드는 문서 전 영역을 덮고 있습니다.

```bash
python examples/build_comparison_report.py    # 사진, 간단한 문서
python examples/fill_form.py                  # .hwp 변환 + 양식 채우기
python examples/build_research_report.py      # 표 6개 + 그림 2장, 5쪽
python examples/edit_existing.py              # 편집 + baseline 비교
```

PR 전에 확인할 것:

1. **네 개가 전부 예외 없이 끝난다**
2. **`verify()`에 새로 생긴 FAIL이 없다** — 고치기 전 출력과 비교합니다
3. **한글에서 눈으로 본다** — 가장 중요합니다. 구조 검사를 다 통과해도 한글에서
   보면 밀려 있거나 겹쳐 있는 경우가 있습니다. 이 저장소 버그의 상당수가 그런
   종류였습니다
4. 문서를 고쳤다면 **문서에 적은 코드를 실제로 실행해 봅니다**. 예제가 틀린 문서는
   없는 문서보다 나쁩니다

편집 기능을 건드렸다면 `baseline=`을 넘긴 검증까지 돌려 주세요. 편집 전용 검사가
거기서만 켜집니다.

```python
print(verify("수정본.hwpx", baseline="원본.hwpx").render())
```

---

## Pull Request

- **하나의 PR은 하나의 일만** 합니다. 버그 수정과 리팩터링을 섞으면 되돌리기
  어려워집니다
- 제목은 무엇을 고쳤는지 한 줄로
- 본문에는 **어떻게 확인했는지**를 적어 주세요 — 돌린 예제, `verify()` 출력,
  한글에서 본 스크린샷
- 화면에 보이는 변화가 있으면 **전/후 스크린샷**을 넣어 주세요. 이 프로젝트에서
  가장 설득력 있는 근거입니다
- 규칙 1(라이선스 경계) 확인 명령 결과를 붙여 주면 리뷰가 빨라집니다

큰 변경은 코드를 쓰기 전에 이슈로 먼저 이야기해 주세요. 특히 새 공개 함수를
추가하는 경우 — 어휘를 작게 유지하는 게 이 도구의 설계 방향이라, 방향이 안 맞으면
작업이 아깝게 됩니다.

작게 시작하셔도 됩니다. 오탈자 수정, 문서 개선, 예제 추가 전부 환영합니다.

---

## 라이선스 동의

PR을 보내면 그 기여가 저장소의 라이선스로 배포되는 데 동의하는 것으로 봅니다.

- `hwpxkit/render.py`, `hwpxkit/convert.py`를 **제외한** 코드 → **Apache-2.0**
- 위 두 파일 → 같은 파일의 기존 조건을 따릅니다

**남의 코드를 가져올 때는 출처와 라이선스를 [`NOTICE`](NOTICE)에 적어 주세요.**
블로그나 스택오버플로에서 가져온 조각도 마찬가지입니다. 라이선스를 모르는 코드는
넣지 않는 게 맞습니다.

---

## 행동 규범

한글 문서 다루다 화날 일이 많은 건 압니다만, 사람에게는 화내지 맙시다.

- 코드와 결과물에 대해 이야기하고, 사람을 평가하지 않습니다
- 초보 질문을 환영합니다. HWPX는 문서가 부족해서 모르는 게 당연합니다
- 정중하지 않은 언행이 있으면 이슈나 siubeom@vetu1.com 으로 알려 주세요

---

궁금한 점은 [이슈](https://github.com/hwpx-builder/hwpx-builder/issues)에 편하게
남겨 주세요. 답이 늦을 수는 있어도 읽기는 다 읽습니다.
